{
  "name": "daily-report",
  "version": "3.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vitepress dev .",
    "index:write": "node scripts/history-index.mjs --write",
    "index:check": "node scripts/history-index.mjs --check",
    "build": "npm run index:check && vitepress build . && node scripts/create-directory-urls.mjs",
    "preview": "vitepress preview ."
  },
  "dependencies": {
    "yaml": "^2.9.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.9.3",
    "vitepress": "^1.6.4"
  }
}
